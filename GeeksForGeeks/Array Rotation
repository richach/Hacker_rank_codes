//rotate by 2 elements
#include<iostream>
using namespace std;
int main()
{
    int num,d;
    cin>>num>>d;
    int arr[num];
    for(int i=0;i<num;i++)
    {
        cin>>arr[i];
    }
    
    for(int i=0;i<num;i++)
    {
        cout<<arr[(i+d)%num]<<endl;
    }
    
}
